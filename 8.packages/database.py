def db_connect(connection:str,port:int):
    return f"{connection} connection is connecting at port{port}"

def db_diconnect(connection:str,port:int):
    return f"{connection} connection is diconnecting at port{port}"
